# =============================================
# MALLORY - Environment Variables Configuration
# =============================================
# Copy this file to `.env` and fill in your actual values.
# Never commit your actual `.env` file to version control!

# =============================================
# REQUIRED - App won't work without these
# =============================================

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Backend API URL (Production Only)
# In development mode, the app automatically uses http://localhost:3001
# For production builds, set this to your deployed backend API
EXPO_PUBLIC_BACKEND_API_URL=https://api.yourdomain.com

# =============================================
# OPTIONAL - For Full Features
# =============================================

# Web OAuth Redirect URL
# Required for web-based Google login
# For local development, use: http://localhost:8081
# For production, use your deployed web URL: https://yourdomain.com
# Defaults to http://localhost:8081 if not set
EXPO_PUBLIC_WEB_OAUTH_REDIRECT_URL=http://localhost:8081

# Google OAuth - iOS
# Required for native iOS Google Sign-In
# Get this from Google Cloud Console: https://console.cloud.google.com
# Create an OAuth 2.0 Client ID for iOS application
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# Google OAuth - Android
# Required for native Android Google Sign-In
# Get this from Google Cloud Console: https://console.cloud.google.com
# Create an OAuth 2.0 Client ID for Android application
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your-android-client-id.apps.googleusercontent.com

# Solana RPC URL
# Required for wallet features
# For production: Use a dedicated RPC provider (QuickNode, Alchemy, etc.)
# Defaults to https://api.mainnet-beta.solana.com if not set
EXPO_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Terms & Privacy URLs (Optional)
# If provided, these will be shown on the login screen
# If omitted, no terms/privacy links will be displayed
EXPO_PUBLIC_TERMS_URL=https://yourdomain.com/terms
EXPO_PUBLIC_PRIVACY_URL=https://yourdomain.com/privacy

# =============================================
# NOTES
# =============================================
#
# 1. EXPO_PUBLIC_* variables:
#    - These are exposed to the client-side app
#    - Never put secrets/private keys here
#    - They're bundled into your app at build time
#
# 2. Backend API:
#    - In development (__DEV__=true), localhost:3001 is used automatically
#    - EXPO_PUBLIC_BACKEND_API_URL only matters for production builds
#
# 3. Google OAuth:
#    - iOS uses EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID for native sign-in
#    - Android uses EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID for native sign-in
#    - Web uses Supabase OAuth with EXPO_PUBLIC_WEB_OAUTH_REDIRECT_URL
#
# 4. AI/LLM Features:
#    - All AI calls should go through your backend API
#    - Your backend should handle Anthropic API calls securely
#    - Never expose AI API keys in the mobile app
#
# 5. Terms & Privacy:
#    - Optional - only displayed if provided
#    - Users can customize for their own legal requirements
#

# Grid Wallet Configuration
# Required for embedded wallet features
# Get your API key from: https://developers.squads.so
EXPO_PUBLIC_GRID_API_KEY=your-grid-api-key-here
EXPO_PUBLIC_GRID_ENV=sandbox
